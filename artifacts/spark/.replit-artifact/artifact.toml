kind = "web"
previewPath = "/"
title = "Spark"
version = "1.0.0"
id = "artifacts/spark"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21615

[services.development]
run = "pnpm --filter @workspace/spark run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/spark", "run", "build" ]
publicDir = "artifacts/spark/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21615"
BASE_PATH = "/"
